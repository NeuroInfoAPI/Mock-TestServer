{
  "name": "nia-mock-test-server",
  "version": "2.1.0",
  "main": "src/index.ts",
  "author": "Appstun",
  "license": "MIT",
  "description": "Bun-based local NeuroInfoApi mock server (REST + WebSocket) with persistent state and upstream import support",
  "scripts": {
    "run": "bun run src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/node": "^25.7.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "neuroinfoapi-client": "^1.5.1"
  },
  "private": true
}
