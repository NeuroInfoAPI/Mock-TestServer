{
  "name": "nia-mock-test-server",
  "version": "2.0.0",
  "main": "src/index.ts",
  "author": "Appstun",
  "license": "MIT",
  "description": "Bun-based local NeuroInfo API mock server (REST + WebSocket) with persistent state and upstream import support",
  "scripts": {
    "run": "bun run src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12",
    "@types/node": "^25.6.0",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "neuroinfoapi-client": "^1.3.4"
  },
  "private": true
}
