{
  "name": "nia-mock-test-server",
  "version": "2.2.0",
  "main": "src/index.ts",
  "author": "Appstun",
  "license": "MIT",
  "description": "Bun-based local NeuroInfoApi mock server (REST + WebSocket) with persistent state and upstream import support",
  "scripts": {
    "run": "bun run src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "neuroinfoapi-client": "2.1.0"
  },
  "private": true
}
